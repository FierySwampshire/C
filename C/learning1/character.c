#include <stdio.h>
#include <stddef.h>
wchar_t wideCharacter = 'τ';//internationalisation, 16 or 32 bit
char character = 'a';//normal char
unsigned char u_char = 1;//allows unsigned integer, incompatible with char
signed char s_char = -1;//allows signed integer assignment, incompatible with char
int main(void){
return 0;
}


